services:
  # PostgreSQL Database
  db:
    image: postgres:16-alpine
    container_name: parivesh_db
    restart: unless-stopped
    environment:
      POSTGRES_USER: parivesh_admin
      POSTGRES_PASSWORD: ${DB_PASSWORD:-parivesh_secure_2024}
      POSTGRES_DB: parivesh_db
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U parivesh_admin -d parivesh_db"]
      interval: 10s
      timeout: 5s
      retries: 5
      start_period: 10s

  # Backend API Server
  backend:
    build:
      context: ./backend
      dockerfile: Dockerfile
    container_name: parivesh_backend
    restart: unless-stopped
    environment:
      NODE_ENV: production
      AUTO_SYNC: "false"
      MIGRATE_ON_START: "true"
      SEED_DEMO_DATA: "true"
      PORT: 5000
      DB_HOST: db
      DB_PORT: 5432
      DB_NAME: parivesh_db
      DB_USER: parivesh_admin
      DB_PASSWORD: ${DB_PASSWORD:-parivesh_secure_2024}
      DB_WAIT_MAX_ATTEMPTS: 30
      DB_WAIT_DELAY_MS: 2000
      JWT_SECRET: ${JWT_SECRET:-change_this_in_production}
      JWT_EXPIRY: 24h
      UPLOAD_DIR: /app/uploads
    ports:
      - "5000:5000"
    volumes:
      - uploads:/app/uploads
    depends_on:
      db:
        condition: service_healthy
    healthcheck:
      test: ["CMD-SHELL", "wget -qO- http://localhost:5000/api/health >/dev/null 2>&1 || exit 1"]
      interval: 15s
      timeout: 5s
      retries: 5
      start_period: 30s

  # Frontend Next.js App
  frontend:
    build:
      context: ./frontend
      dockerfile: Dockerfile
    container_name: parivesh_frontend
    restart: unless-stopped
    environment:
      NEXT_PUBLIC_API_URL: http://localhost:5000/api
      # Internal Docker network URL used by the Next.js proxy API routes
      BACKEND_INTERNAL_URL: http://backend:5000/api
    ports:
      - "3000:3000"
    depends_on:
      backend:
        condition: service_healthy
    healthcheck:
      test: ["CMD-SHELL", "wget -qO- http://localhost:3000 >/dev/null 2>&1 || exit 1"]
      interval: 15s
      timeout: 5s
      retries: 5
      start_period: 30s

volumes:
  pgdata:
  uploads:
